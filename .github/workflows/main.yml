- name: Markdown SEO Check
  # You may pin to the exact commit or the version.
  # uses: zentered/markdown-seo-check@860fdc7a5cef83835033ffb6615cfb8d8c36ee0b
  uses: zentered/markdown-seo-check@v1.0.0
  with:
    # Maximum length for page title
    max_title_length: # default is 70
    # Maximum length for description
    max_description_length: # default is 150
    # Maximum length for slug
    max_slug_length: # default is 100
